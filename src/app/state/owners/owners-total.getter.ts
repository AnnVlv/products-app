import {Product, ProductsStateModel} from '../../models';
import {ProductsState} from '../products/poducts.state';


// Object.assign(ProductsState, {
//   total: function({ products, productsIds }: ProductsStateModel): number {
//     return productsIds.reduce((total, id) => total + products[id].total, 0);
//   }
// });

// ProductsState.total = function({ products, productsIds }: ProductsStateModel): number {
//   return productsIds.reduce((total, id) => total + products[id].total, 0);
// };
